---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: cgicloudnative/concourse-alpine
      #certbot/dns-route53
 
inputs:
- name: certbot-dns-repo
outputs:
- name: certbot-repo


params:
  AWS_ACCESS_KEY_ID:
  AWS_SECRET_ACCESS_KEY:
  EMAIL_ADDRESS:
  DOMAINS:

run:
  path: certbot-dns-repo/pipeline/generate_cert.sh
